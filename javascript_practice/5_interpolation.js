const username = 'Kenn';

console.log (`${username} is he true JS Wizard!`);

console.log ('Hey students, What bugs can ${username} fix for you today');

const person = {
    username: 'Kenn',
    profession: 'Secret Agent'
}

console.log(`${person.username}, ${person.profession}`, 'extraordinaire'); 